export class Team {
    constructor(
        public _id: string,
        public name: string,
        public image_flag: string,
        public image_shield: string,
        public user: string
    ) {}
}
